# AI Agent Khalid — v5.5

نسخة موحدة قبل الرفع إلى GitHub، تشمل إصلاح التصنيف + الأوامر الصوتية.

## الجديد
- زر ميكروفون 🎤 داخل شريط الأدوات.
- أوامر صوتية عربية/إنجليزية عبر SpeechRecognition في المتصفح.
- أمثلة أوامر: اعرض الرسائل التي تحتاج رد، رسائل المتابعة، لا يحتاج إجراء، كل الرسائل، تحديث البريد، افتح آخر رسالة، أنشئ مسودة، افتح الإعدادات، ابحث عن ...
- يظهر النص الذي تم سماعه داخل التطبيق.
- عند أمر «أنشئ مسودة» ينشئ Draft فقط في Gmail ولا يرسل.
- الإرسال التلقائي يبقى مقفولًا.
- يحتفظ بجميع مزايا v5.4 ومنها إصلاح تصنيف الأسئلة/المواعيد على أنها تحتاج رد.

## ملاحظة iPhone
الميزة تعتمد على دعم Safari/iOS لـ SpeechRecognition وعلى منح الموقع إذن الميكروفون. الموقع يعمل عبر HTTPS على Render، وهو مطلوب للميكروفون.

## ملفات الرفع
ارفع الملفات الموجودة في هذا المجلد إلى GitHub، ولا ترفع `__pycache__` إن ظهر محليًا.
